syntax = "proto3";

message Heads {
  repeated bytes cids = 1;
  optional bool comprehensive = 2;
}

message RequestHeads {
  bytes filter = 1;
  uint32 hashes = 2;
  optional uint32 seed = 3;
  optional bytes hash = 4;
}
